# Avshalom-Ts dotfile

## Usage

1. Make shure stow is installed, if not `sudo apt install stow`

2. Clone the repo: `git clone https://github.com/Avshalom-Ts/dotfiles.git`

3. Run the script then stow
